<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hstk.iot_warehouse.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.hstk.iot_warehouse.model.entity.SysUser">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="status" property="status"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.hstk.iot_warehouse.model.entity.SysUser" useGeneratedKeys="true"
            keyProperty="userId">
        INSERT INTO sys_user (username, password, nickname, status, role, create_time)
        VALUES (#{username}, #{password}, #{nickname}, #{status}, #{role}, #{createTime})
    </insert>

    <update id="update" parameterType="com.hstk.iot_warehouse.model.entity.SysUser">
        UPDATE sys_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="status != null">status = #{status},</if>
            <if test="role != null">role = #{role},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE
        FROM sys_user
        WHERE user_id = #{userId}
    </delete>

    <select id="selectById" resultMap="BaseResultMap" parameterType="long">
        SELECT user_id, username, password, nickname, status, role, create_time
        FROM sys_user
        WHERE user_id = #{userId}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap" parameterType="String">
        SELECT user_id, username, password, nickname, status, role, create_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT *
        FROM sys_user
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <!-- 更新密码 -->
    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户状态 -->
    <update id="updateStatus">
        UPDATE sys_user
        SET status = #{status}
        WHERE user_id = #{userId}
    </update>
</mapper>